# See https://redocly.com/docs/cli/configuration/ for more information.
apis:
  sample@v1: 
    root: openapi/openapi-v1.yaml
  sample@v2: 
    root: openapi/openapi-v2.yaml
lint:
  extends:
    - recommended
  rules:
    no-unused-components: error
features.openapi:
  showConsole: false
  htmlTemplate: ./docs/index.html
  theme:
    colors:
      primary:
        main: "#32329f"
